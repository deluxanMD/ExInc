import { SafeAreaProvider } from 'react-native-safe-area-context';
import styled from 'styled-components';

export const SAProvider = styled(SafeAreaProvider)`
  padding-left: 10px;
  padding-right: 10px;
`;
